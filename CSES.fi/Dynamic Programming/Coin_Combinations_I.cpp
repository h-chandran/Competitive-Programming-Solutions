#include <iostream>
#include <vector>
#include <algorithm>
#include <climits>

using namespace std;
#define ll long long
#define MOD 1000000007


vector<int> coins;

vector<int> arr(2000001, 0);
 
int main(){
    int n, x; cin >> n >> x;
    for(int i = 0; i < n; i++){
        ll c; cin >> c; coins.push_back(c);
    }
}